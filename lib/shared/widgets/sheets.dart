import 'package:flutter/material.dart';

// Reusable sheets
class AppSheets extends StatelessWidget {
  const AppSheets({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
