name: pingo
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^8.0.1
  go_router: ^17.1.0
  flutter_riverpod: ^3.1.0
  riverpod_annotation: ^4.0.0
  # Networking
  dio: ^5.4.0
  # Database (Local-first)
  drift: ^2.14.0
  drift_flutter: ^0.2.8
  sqlite3_flutter_libs: ^0.5.18
  path_provider: ^2.1.1
  path: ^1.9.0
  # Permissions
  permission_handler: ^12.0.1
  flutter_map: ^8.2.2
  latlong2: ^0.9.1
  geolocator: ^14.0.2
  intl: ^0.20.2
  flutter_map_tile_caching: ^10.1.1
  image_picker: ^1.2.1
  cross_file: ^0.3.5+2
  share_plus: ^12.0.1
  # Maps (Placeholder for now)
  # flutter_map: ^6.0.0 # Using open source alternative for now to avoid key setup initially

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  riverpod_generator: ^4.0.0+1
  build_runner: ^2.4.8
  drift_dev: ^2.14.0

flutter:
  uses-material-design: true
